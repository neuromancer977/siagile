CLI e' la command line interface

Esiste in 1000 varianti ed e' una generalizzazione che mi e' venuta in mente
partendo da getopt.h di <unistd> di C/C++ e apache commons CLI.

E' solo una prima bozza per gestire i comandi e le opzioni.
Identifica solo il domain model.

Vorrebbe gestire anche i path che ora mancano per validare il comando e altre cosette.
A riguardo rimando alla letteratura.